/* Generated by TestGen on Thu Mar 24 2022 at 11:20 AM (GMT) */
@isTest
private class HttpConsult_TGN_TEST{
    private class HttpConsult_HCM_TGN_TEST implements HttpCalloutMock{
        protected Integer code;
        protected String status;
        protected String body;
        protected Map<String,String> headers;
        public HttpConsult_HCM_TGN_TEST(Integer code,String status,String body,Map<String,String> headers){
            this.code=code;
            this.status=status;
            this.body=body;
            this.headers=headers;
        }
        public HTTPResponse respond(HTTPRequest req){
            HttpResponse res=new HttpResponse();
            res.setStatusCode(this.code);
            res.setStatus(this.status);
            res.setBody(this.body);
            if(this.headers!=null){
                for(String key:this.headers.keySet()){
                    res.setHeader(key,this.headers.get(key));
                }
            }
            return res;
        }
    }
    @isTest
    private static void request_TGN_TEST(){
        Test.startTest();
        HttpConsult_HCM_TGN_TEST httpMock;
        httpMock=new HttpConsult_HCM_TGN_TEST(200,'Complete','{}',null);
        Test.setMock(HttpCalloutMock.class,httpMock);
        HttpConsult obj = new HttpConsult();
        obj.request('a','a','a',new Map<String,String>());
        httpMock=new HttpConsult_HCM_TGN_TEST(200,'Complete','{}',null);
        Test.setMock(HttpCalloutMock.class,httpMock);
        obj = new HttpConsult();
        obj.request('a','a','a',new Map<String,String>{'a'=>'a'});
        Test.stopTest();
    }
}