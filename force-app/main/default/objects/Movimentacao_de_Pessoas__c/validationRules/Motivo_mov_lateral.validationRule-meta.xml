<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Motivo_mov_lateral</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    TEXT(Motivo1__c) = &quot;Movimentação Lateral (não há mudança de salário, apenas de cargo)&quot;,
    OR(
        ISBLANK(Cargo__c),
        NOT(ISBLANK(Novo_Sal_rio__c))
    )
)</errorConditionFormula>
    <errorMessage>Movimentação Lateral (não há mudança de salário, apenas de cargo)</errorMessage>
</ValidationRule>
