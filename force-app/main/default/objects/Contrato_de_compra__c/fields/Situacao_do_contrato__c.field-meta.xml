<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Situacao_do_contrato__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    ISNULL(Final_do_contrato__c), 
    &quot;PRAZO INDETERMINADO&quot;,
    IF(
        Final_do_contrato__c &lt; TODAY(), 
        &quot;VENCIDO&quot;,
        IF(
            Final_do_contrato__c &lt;= TODAY() + 30, 
            &quot;A VENCER EM 30 DIAS&quot;,
            IF(
                Final_do_contrato__c &lt;= TODAY() + 60, 
                &quot;A VENCER EM 60 DIAS&quot;,
                IF(
                    ISNULL(Inicio_do_contrato__c), 
                    &quot;ERRO&quot;,
                    &quot;VIGENTE&quot;
                )
            )
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Situação do contrato</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
