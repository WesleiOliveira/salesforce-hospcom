<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Tx_juros_s_entrada__c</fullName>
    <description>Taxa de juros da parcela calculado com o juros base.
Esse juros é base para calcular o maxima de desconto permitdo levantando em conta a despesa financeira.</description>
    <externalId>false</externalId>
    <formula>IF(
  Numero_de_Parcelas_formula__c = 1,
  0,
  IF(
    AND(TotalPrice &gt; 0, Numero_de_Parcelas_formula__c &gt; 0, Juros_Base__r.Juros__c &gt; 0),
    (
      (
        (TotalPrice * (Juros_Base__r.Juros__c / (1 - EXP(-Numero_de_Parcelas_formula__c * LN(1 + Juros_Base__r.Juros__c)))))
        * Numero_de_Parcelas_formula__c 
      ) - TotalPrice
    ) / TotalPrice,
    0
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Taxa de Juros sem entrada</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Percent</type>
</CustomField>
