<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Numero_de_Parcelas_formula__c</fullName>
    <description>Retorna o numero de parcelas</description>
    <externalId>false</externalId>
    <formula>IF(
    TEXT(Condicao_de_pagamento__c) = &quot;90 Dias&quot;,
    3,
    IF(
        TEXT(Condicao_de_pagamento__c) = &quot;60 Dias&quot;,
        2,
        IF(
            CONTAINS(TEXT(Condicao_de_pagamento__c), &quot;x&quot;),
            VALUE(
                LEFT(
                    TEXT(Condicao_de_pagamento__c),
                    FIND(&quot;x&quot;, TEXT(Condicao_de_pagamento__c)) - 1
                )
            ),
            1
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Número de Parcelas(formula)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
