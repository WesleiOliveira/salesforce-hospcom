<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Resposta_a_notificacao</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    $User.Id != &apos;0055A00000BcBOn&apos;,
    OR(
        ISNEW(),
        ISCHANGED(ActivityDate),
        ISCHANGED(Subject),
        ISCHANGED(OwnerId)
    ),
Subject = &apos;Resposta à Notificação&apos;,
    IF(
        WEEKDAY(TODAY()) = 6,
        ActivityDate - 5 &lt; TODAY(),
        IF(
            WEEKDAY(TODAY()) = 5,
            ActivityDate - 5 &lt; TODAY(),
            IF(
                WEEKDAY(TODAY()) = 4,
                ActivityDate - 3 &lt; TODAY(),
                IF(
                    WEEKDAY(TODAY()) = 3,
                    ActivityDate - 3 &lt; TODAY(),
                    IF(
                        WEEKDAY(TODAY()) = 2,
                        ActivityDate - 3 &lt; TODAY(),
                        IF(
                            WEEKDAY(TODAY()) = 1,
                            ActivityDate - 2 &lt; TODAY(),
                            true
                        )
                    )
                )
            )
        )
    )
)</errorConditionFormula>
    <errorDisplayField>ActivityDate</errorDisplayField>
    <errorMessage>O prazo mínimo para denúncia é de dois dias úteis.</errorMessage>
</ValidationRule>
