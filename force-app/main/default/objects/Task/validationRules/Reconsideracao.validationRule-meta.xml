<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Reconsideracao</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    $User.Id != &apos;0055A00000BcBOn&apos;,
    $User.Id != OwnerId,
    OR(
        ISNEW(),
        ISCHANGED(ActivityDate),
        ISCHANGED(OwnerId)
    ),
    Subject = &apos;Reconsideração&apos;,
    IF(
        WEEKDAY(TODAY()) = 6,
        ActivityDate - 4 &lt; TODAY(),
        IF(
            WEEKDAY(TODAY()) = 5,
            ActivityDate - 4 &lt; TODAY(),
            IF(
                WEEKDAY(TODAY()) = 4,
                ActivityDate - 2 &lt; TODAY(),
                IF(
                    WEEKDAY(TODAY()) = 3,
                    ActivityDate - 2 &lt; TODAY(),
                    IF(
                        WEEKDAY(TODAY()) = 2,
                        ActivityDate - 2 &lt; TODAY(),
                        IF(
                            WEEKDAY(TODAY()) = 1,
                            ActivityDate - 2 &lt; TODAY(),
                            true
                        )
                    )
                )
            )
        )
    )
)</errorConditionFormula>
    <errorDisplayField>ActivityDate</errorDisplayField>
    <errorMessage>O prazo mínimo para reconsideração é de dois dias úteis.</errorMessage>
</ValidationRule>
