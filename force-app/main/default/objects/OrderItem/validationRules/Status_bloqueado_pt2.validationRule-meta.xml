<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Status_bloqueado_pt2</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
	!$User.Ignorar_erros__c,
	ISCHANGED(Status__c),
	TEXT(Order.Status) == &apos;Cancelado&apos;
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>Pedido cancelado por completo não retorna o status. Considere criar novo pedido.</errorMessage>
</ValidationRule>
