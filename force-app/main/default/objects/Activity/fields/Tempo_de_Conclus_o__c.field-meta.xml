<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Tempo_de_Conclus_o__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
  AND(
    NOT(ISBLANK(CreatedDate)),
    NOT(ISBLANK(CompletedDateTime))
  ),
  ROUND(
    ( 
      ( (CompletedDateTime - CreatedDate) * 1440 ) - 
      IF(ISBLANK(Tempo_parado__c), 0, Tempo_parado__c)
    ) / 1440,
    2
  ),
  NULL
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Tempo de Conclusão (Dias)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
