<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Faturado__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
	AND(TotalAmount == 0,Total_Faturado__c==0, OR(Quantidade_de_faturamentos__c == null, Quantidade_de_faturamentos__c == 0)),
	0,
	IF(AND(TotalAmount == 0, Quantidade_de_faturamentos__c &gt;= 1), 1,

        (Total_Faturado__c / TotalAmount))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Faturado (%)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Percent</type>
</CustomField>
