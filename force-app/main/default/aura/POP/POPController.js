({
    onRender: function(cmp, event, helper) {
        helper.helperMethod(cmp, event, helper);
    },
    buscaIs: function(cmp, event, helper) {
        helper.buscaIs(cmp, event, helper);
    },
    adicionarTag: function(cmp, event, helper) {
        helper.adicionarTag(cmp, event, helper);
    },
    removerTag: function(cmp, event, helper) {
        helper.removerTag(cmp, event, helper);
    }
})